# Git Submodules

git submodule add git://repo .vim/bundler/repo
git submodule init
git submodule update 
git submodule foreach git pull origin master 

# Git remove submodule
git rm --cached /path/to/module
git config -f .git/config --remove-section submodule.path_to_module
git config -f .gitmodules --remove-section submodule.path_to_module

# Color schemes  1/10
# Schema        | JavaScript | Coffee-Script | Ruby | PHP | HTML | CSS-LESS
* Rails Envy    |            |               |      |     |      |        
* Solarized     |            |               |      |     |      |
* Monokai       |            |               |      |     |      |
* IR_black      | 4          |               |      | 5   |      |                 

# Bash tricks
<Ctrl+e>        - Move the cursor to the end of the line.
cd -            - Return you to the previous directory.

# Notes for feature self
  * work more with tmux
  * or try byobu
